- name: My first play
  hosts: webserver1
  tasks:
   - name: Ping my hosts
     ansible.builtin.ping:

             #   - name: Print message
             #     ansible.builtin.debug:
             #     msg: Hello world
